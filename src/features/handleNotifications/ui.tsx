import React from 'react';import Stack from '@mui/material/Stack';import MuiAlert, { AlertProps } from '@mui/material/Alert';import { closeNotification } from './model';import { INotification } from '../../entities/notifications';interface INotificationProps {  notifications: INotification[];}export const Notifications: React.FC<INotificationProps> = React.memo(({ notifications }) => {  return (    <Stack spacing={2} sx={{ width: '100%' }}>      {        notifications.map(({ kind, message, tableId, productId }) => (          <Alert            onClose={() => closeNotification({ kind, message, tableId, productId })}            severity={`${kind}`}            sx={{ width: '100%' }}          >            {message}          </Alert>        ))      }    </Stack>  );});const Alert = React.forwardRef<HTMLDivElement, AlertProps>(function Alert(  props,  ref,) {  return <MuiAlert elevation={6} ref={ref} variant="filled" {...props} />;});